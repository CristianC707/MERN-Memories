import React from "react";

import useStyles from './style';


const Form = () => {
    const classes = useStyles();
    return (
        <h1 className={classes}>FORM</h1>
    );
}

export default Form;